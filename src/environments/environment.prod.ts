/*export const environment = {
  production: true
};*/

// const url = 'http://localhost:53069';

const url = 'https://www.isy.mobi/isywebadmin2';

export const environment = {
  production: true,
  url: url,
  apiUrl: url + '/api'
};
